<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expensetracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IndividualMenuActivity" />
        <activity android:name=".GroupMenu" />
        <activity android:name=".AddGroup" />
        <activity android:name=".AddIndividual" />
        <activity android:name=".EventPage" />
        <activity android:name=".AddEventSpending" />
        <activity android:name=".MainMenu" />
        <activity android:name=".Register" />
        <activity android:name=".ManageMember"></activity>
    </application>

</manifest>